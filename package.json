{
  "name": "nuxt-app",
  "version": "2.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": "20.9.0"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "dev:host": "nuxt dev --host",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy:gh": "nuxt build && nuxt generate && gh-pages -d dist"
  },
  "dependencies": {
    "nuxt": "^3.11.2",
    "vue": "^3.4.21",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "dayjs-nuxt": "^2.1.9",
    "gh-pages": "^6.1.1",
    "nuxt-svgo": "^4.0.0",
    "sass": "^1.75.0"
  }
}
